import { GithubRepo } from "./parseRepoUrl";

export async function ListPullRequests(repo: GithubRepo, per_page: number, page: number): Promise<any> {

}

export async function GetPullRequest(repo: GithubRepo, pr_number: number): Promise<any> {

}